'use strict';

/**
 * facility-operator-contact router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::facility-operator-contact.facility-operator-contact');
