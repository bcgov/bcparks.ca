'use strict';

/**
 * facility-operator-contact service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::facility-operator-contact.facility-operator-contact');
