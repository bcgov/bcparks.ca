"use strict";

/**
 * protected-area controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::protected-area.protected-area");
