"use strict";

/*
  This migration generates name fields for park gates based on related park entities.
  In the future these will be generated by the name-generator middleware, but this
  one-time migration is needed to populate all existing records with name values.
*/

module.exports = {
  async up(knex) {
    if (
      (await knex.schema.hasTable("park_gates")) &&
      (await knex.schema.hasColumn("park_gates", "name"))
    ) {
      await knex.raw(`
        WITH name_build AS (
          SELECT
            pd.id,
            COALESCE(p.orcs::text, '') || ': ' ||
            COALESCE(p.protected_area_name, '') AS new_name
          FROM public.park_gates AS pd
          LEFT JOIN public.park_gates_protected_area_lnk AS pdfl ON pdfl.park_gate_id = pd.id
          LEFT JOIN public.protected_areas AS p ON p.id = pdfl.protected_area_id
          WHERE p.orcs IS NOT NULL
        )
        UPDATE public.park_gates AS pd
        SET name = nb.new_name
        FROM name_build AS nb
        WHERE nb.id = pd.id;
      `);
      await knex.raw(`
        WITH name_build AS (
          SELECT
            pd.id,
            COALESCE('PA-' || pf.orcs_area_number::text, '') || ': ' ||
            COALESCE(pf.park_area_name::text, '') AS new_name
          FROM public.park_gates AS pd
          LEFT JOIN public.park_gates_park_area_lnk AS pdfl ON pdfl.park_gate_id = pd.id
          LEFT JOIN public.park_areas AS pf ON pf.id = pdfl.park_area_id
          WHERE pf.orcs_area_number IS NOT NULL
        )
        UPDATE public.park_gates AS pd
        SET name = nb.new_name
        FROM name_build AS nb
        WHERE nb.id = pd.id;
      `);
      await knex.raw(`
        WITH name_build AS (
          SELECT
            pd.id,
            COALESCE('F-' || pf.orcs_feature_number::text, '') || ': ' ||
            COALESCE(pf.park_feature_name::text, '') AS new_name
          FROM public.park_gates AS pd
          LEFT JOIN public.park_gates_park_feature_lnk AS pdfl ON pdfl.park_gate_id = pd.id
          LEFT JOIN public.park_features AS pf ON pf.id = pdfl.park_feature_id
          WHERE pf.orcs_feature_number IS NOT NULL
        )
        UPDATE public.park_gates AS pd
        SET name = nb.new_name
        FROM name_build AS nb
        WHERE nb.id = pd.id;
      `);
    }
  },
};
