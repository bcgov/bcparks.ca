{
  "routes": [
    {
      "method": "GET",
      "path": "/legacy-redirects",
      "handler": "legacy-redirect.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/legacy-redirects/count",
      "handler": "legacy-redirect.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/legacy-redirects/:id",
      "handler": "legacy-redirect.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/legacy-redirects",
      "handler": "legacy-redirect.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/legacy-redirects/:id",
      "handler": "legacy-redirect.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/legacy-redirects/:id",
      "handler": "legacy-redirect.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
