/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import AppDashboard from './AppDashboard';

storiesOf('AppDashboard', module).add('default', () => <AppDashboard />);
